<form method="post" action="{{ path('app_country_delete', {'id': country.id}) }}" onsubmit="return confirm('Вы уверены что хотите удалить?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ country.id) }}">
    <button class="btn btn-red">Удалить</button>
</form>
