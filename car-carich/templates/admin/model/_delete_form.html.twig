<form method="post" action="{{ path('app_model_delete', {'id': model.id}) }}" onsubmit="return confirm('Вы уверены что хотите удалить?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ model.id) }}">
    <button class="btn btn-red">Удалить</button>
</form>
