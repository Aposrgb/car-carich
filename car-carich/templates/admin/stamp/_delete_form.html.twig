<form method="post" action="{{ path('app_stamp_delete', {'id': stamp.id}) }}" onsubmit="return confirm('Вы уверены что хотите удалить?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stamp.id) }}">
    <button class="btn btn-red">Удалить</button>
</form>
