<style>
    :root {
        --secondary-text-color: #757575;
    }

    .header-color{
        background: var(--default-page-color);
    }

    .header {
        display: block;
        font-size: 13px;
    }

    .header-block{
        padding: 30px 0;
        display: grid;
        gap: 20px;
        grid-template-columns: calc(20% - 16px) calc(15% - 16px) calc(27.5% - 16px) calc(27.5% - 16px) calc(10% - 16px);
    }

    .header-img{
        width: 23px;
    }

    .header a {
        color: unset;
    }

    .header-logo-text{
        font-size: 25px;
        font-weight: bold;
    }

    .header-secondary-text{
        font-size: 16px;
        color: var(--secondary-text-color);
    }

    .header-logo{
        width: 50px;
    }
    .header-signin{
        font-weight: 600;
        color: #212121;
    }

    .header-base-btn {
        height: 62px;
        background: var(--default-white-color);
        border-radius: 2px;
    }

    .header-small-btn {
        padding: 0 45px;
    }

    .header-avg-btn {
        padding: 0 50px;
    }

    .header-btn {
        padding: 0 95px;
    }

    .header-btn-accent{
        background: var(--default-accent-color);
    }

    @media only screen and (max-width: 768px) {
        .header {
            font-size: 6px;
        }

        .header-logo {
            width: 20px;
        }
    }

    @media only screen and (max-width: 400px) {
        .header-text > a{
            margin: 0 5px;
        }
    }
</style>