<style>
    .call-btn-catalog-container{
        display: grid;
        grid-template-columns: calc(25% - 22.5px) calc(75% - 8px);
    }

    .call-btn-accent{
        padding: 15px 0;
        background: var(--default-accent-color);
    }

    .swiper-container {
        width: 100%;
        height: 100%; /* Задайте высоту по необходимости */
        position: relative;
        overflow: hidden;
    }

    /* Контейнер для слайдов */
    .swiper-wrapper {
        display: flex;
        flex-direction: row;
        width: 100%;
    }

    /* Каждый слайд */
    .swiper-slide {
        flex: 1 0 auto; /* Это гарантирует, что каждый слайд занимает полную ширину */
        width: 100%;    /* Каждый слайд занимает всю ширину контейнера */
        text-align: center;
    }

    .swiper-slide img {
        width: 100%;
        height: auto; /* Поддерживает пропорции изображений */
    }


    .custom-pagination {
        bottom: 0;
        z-index: 11;
        margin: 20px 40px;
    }

    .custom-bullet {
        position: relative;
        display: inline-block;
        width: 20px;
        height: 4px;
        margin: 0 5px;
        background-color: grey;
        border-radius: 2px;
    }

    .custom-bullet.active {
        opacity: 1;
        width: 40px;
    }

    .loader {
        position: absolute;
        top: 0;
        left: 0;
        width: 0;
        height: 100%;
        background-color: white;
        border-radius: 5px;
        animation: load 3s linear forwards; /* Настраиваем анимацию загрузки */
    }

    /* Анимация лоадера */
    @keyframes load {
        from {
            width: 0;
        }
        to {
            width: inherit;
        }
    }

    .popular-cars.grid-pop > div > span {
        display: none;
    }

</style>